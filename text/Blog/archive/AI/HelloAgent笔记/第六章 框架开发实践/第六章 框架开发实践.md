# 第六章 框架开发实践

这一节写的很好: [第六章 框架开发实践](https://datawhalechina.github.io/hello-agents/#/./chapter6/%E7%AC%AC%E5%85%AD%E7%AB%A0%20%E6%A1%86%E6%9E%B6%E5%BC%80%E5%8F%91%E5%AE%9E%E8%B7%B5)

这里就简单做下总结.

主要介绍了四种agent框架

![image](assets/image-20260318160338-6vekbp9.png)

# AutoGen

![image](assets/image-20260318162306-tqz2a6y.png)

拉一个群组, 将所有agent都拉进来, 然后按照规定的顺序在群组内发言, 直到完成任务.

你可以通过提示词来让这些agent分别扮演不同的角色, 规定他们的输出, 等等.

agent类型UserProxyAgent和AssistantAgent. 简单来说就是用户跟UserProxyAgent对话, UserProxyAgent跟AssistantAgent在群里聊天, 然后如果有需要, UserProxyAgent会跟你报告.

优点是非常简单好理解, 使用很方便, 缺点是对话形式下, llm的回复可能偏理预期, 导致异常结果或循环; 并且debug困难, 只有对话历史可以看, 这被称为“对话式调试”的难题。

# AgentScope

消息驱动. 整个框架非常大而全, 工业级别. 看下面的框架图就知道了, 很复杂.

![image](assets/image-20260318162328-gqxyjtr.png)

所有智能体交互都被抽象为消息的发送和接收, 从而天然支持了:

- ​**异步解耦**: 消息的发送方和接收方在时间上解耦，无需相互等待，天然支持高并发场景。
- ​**位置透明**: 智能体无需关心另一个智能体是在本地进程还是在远程服务器上，消息系统会自动处理路由。
- ​**可观测性**: 每一条消息都可以被记录、追踪和分析，极大地简化了复杂系统的调试与监控。
- ​**可靠性**: 消息可以被持久化存储和重试，即使系统出现故障，也能保证交互的最终一致性，提升了系统的容错能力。

消息具有标准结构

```python
from agentscope.message import Msg

# 消息的标准结构
message = Msg(
    name="Alice",           # 发送者名称
    content="Hello, Bob!",  # 消息内容
    role="user",           # 角色类型
    metadata={             # 元数据信息
        "timestamp": "2024-01-15T10:30:00Z",
        "message_type": "text",
        "priority": "normal"
    }
)
```

agent的生命周期由基类AgentBase统一管理. 只需关心核心的reply等方法.

```python
from agentscope.agents import AgentBase

class CustomAgent(AgentBase):
    def __init__(self, name: str, **kwargs):
        super().__init__(name=name, **kwargs)
        # 智能体初始化逻辑
    
    def reply(self, x: Msg) -> Msg:
        # 智能体的核心响应逻辑
        response = self.model(x.content)
        return Msg(name=self.name, content=response, role="assistant")
    
    def observe(self, x: Msg) -> None:
        # 智能体的观察逻辑（可选）
        self.memory.add(x)
```

消息中心MsgHub, 管理消息的路由和分发(广播等), 还集成了持久化(将消息自动保存到数据库)和分布式通信(agent可被部署到不同服务器或进程, 通过rpc处理同薪)等高级功能

原文的案例是写一个狼人杀游戏.

不同的agent按照角色被分类到不同数组, 比如狼人数组, 预言家数组.

然后天黑阶段, 通过msgHub临时创建一个专属于狼人数组的hub, 

```python
async def werewolf_phase(self, round_num: int):
    """狼人阶段 - 展示消息驱动的协作模式"""
    if not self.werewolves:
        return None
        
    # 通过消息中心建立狼人专属通信频道
    async with MsgHub(
        self.werewolves,
        enable_auto_broadcast=True,
        announcement=await self.moderator.announce(
            f"狼人们，请讨论今晚的击杀目标。存活玩家：{format_player_list(self.alive_players)}"
        ),
    ) as werewolves_hub:
        # 讨论阶段：狼人通过消息交换策略
        for _ in range(MAX_DISCUSSION_ROUND):
            for wolf in self.werewolves:
                await wolf(structured_model=DiscussionModelCN)
        
        # 投票阶段：收集并统计狼人的击杀决策
        werewolves_hub.set_auto_broadcast(False)
        kill_votes = await fanout_pipeline(
            self.werewolves,
            msg=await self.moderator.announce("请选择击杀目标"),
            structured_model=WerewolfKillModelCN,
            enable_gather=False,
        )
```

可以看到有一个: `await wolf(structured_model=DiscussionModelCN)`​ 意思是使用**结构化输出**.

结构化输出是agent scope的agent-level infrastructure中的一个功能(在架构图中可以看到).

就是说, 我定义一个结构化输出类, 然后调用agent的时候把这个类传给agent, agent会根据传入的prompt, 让大模型按照这个结构化输出类来生成内容, 并且填写这个结构化输出类中的字段, 然后将字段存到输出msg的metadata中.

比如下面就是一个结构化输出类. 它是基于Pydantic的BaseModel类实现的.

```python
class WitchActionModelCN(BaseModel):
    """女巫行动的输出格式"""
    use_antidote: bool = Field(description="是否使用解药")
    use_poison: bool = Field(description="是否使用毒药")
    target_name: Optional[str] = Field(description="毒药目标玩家姓名")
```

女巫阶段中, 就获取了metadata中的target_name等字段, 来推进游戏. 

```python
        # 检查返回结果是否有效
        if witch_action is None or not hasattr(witch_action, 'metadata') or witch_action.metadata is None:
            print(f"⚠️ 女巫行动失败,视为不使用技能")
        else:
            if witch_action.metadata.get("use_antidote") and self.witch_has_antidote:
                if killed_player:
                    saved_player = killed_player
                    self.witch_has_antidote = False
                    await witch_agent.observe(await self.moderator.announce(f"你使用解药救了{killed_player}"))

            if witch_action.metadata.get("use_poison") and self.witch_has_poison:
                poisoned_player = witch_action.metadata.get("target_name")
                if poisoned_player:
                    self.witch_has_poison = False
                    await witch_agent.observe(await self.moderator.announce(f"你使用毒药毒杀了{poisoned_player}"))
        
```

结构化输出相当重要. 在整个程序中贯彻始终, 它能够很大程度上削减llm回复的不确定性.

可以通过`fanout_pipeline`对某个数组中的agent广播消息, 然后收集他们的msg, 进而推进程序, 比如"投票".

可以通过`sequential_pipeline`让某个数组中的agent按顺序发言.

```python
    async def day_phase(self, round_num: int):
        """白天阶段"""
        await self.moderator.day_announcement(round_num)
        
        # 讨论阶段
        async with MsgHub(
            self.alive_players,
            enable_auto_broadcast=True,
            announcement=await self.moderator.announce(
                f"现在开始自由讨论。存活玩家：{format_player_list(self.alive_players)}"
            ),
        ) as all_hub:
            # 每人发言一轮
            await sequential_pipeline(self.alive_players)
            
            # 投票阶段
            all_hub.set_auto_broadcast(False)
            vote_msgs = await fanout_pipeline(
                self.alive_players,
                await self.moderator.announce("请投票选择要淘汰的玩家"),
                structured_model=get_vote_model_cn(self.alive_players),
                enable_gather=False,
            )
            
            # 统计投票
            votes = {}
            for i, vote_msg in enumerate(vote_msgs):
                # 检查vote_msg是否为None或metadata是否存在
                if vote_msg is not None and hasattr(vote_msg, 'metadata') and vote_msg.metadata is not None:
                    votes[self.alive_players[i].name] = vote_msg.metadata.get("vote")
                else:
                    # 如果返回无效,默认弃票
                    print(f"⚠️ {self.alive_players[i].name} 的投票无效,视为弃票")
                    votes[self.alive_players[i].name] = None
            
            voted_out, vote_count = majority_vote_cn(votes)
            await self.moderator.vote_result_announcement(voted_out, vote_count)
            
            return voted_out
```

可以看到, AgentScope的整个框架复杂度明显高于autogen, 其消息驱动的架构让整个应用更加可控, 易debug, 也非常灵活可靠.

# CAMEL

主要是角色扮演+引导性提示.

一共有两个agent: AI用户和AI助理. 他们分别扮演一个角色, AI用户负责提出需求、下达指令和构思任务步骤, AI助理负责根据指令执行具体操作和提供解决方案。

引导性提示就类似system prompt, 它通常包含以下几个关键部分：

- ​**明确自身角色**：例如，“你是一位资深的股票交易员...”
- ​**告知协作者角色**：例如，“你正在与一位优秀的 Python 程序员合作...”
- ​**定义共同目标**：例如，“你们的共同目标是开发一个股票交易策略分析工具。”
- ​**设定行为约束和沟通协议**​：这是最关键的一环。例如，指令会要求 AI 用户“一次只提出一个清晰、具体的步骤”，并要求 AI 助理“在完成上一步之前不要追问更多细节”，同时规定双方需在回复的末尾使用特定标志（如 `<SOLUTION>`）来标识任务的完成。

![image](assets/image-20260318200139-vta1svk.png)

CAMEL 最大的优势在于其"轻架构、重提示"的设计哲学。相比 AutoGen 的复杂对话管理和 AgentScope 的分布式架构，CAMEL 通过精心设计的初始提示就能实现高质量的智能体协作。这种自然涌现的协作行为，往往比硬编码的工作流更加灵活和高效。

值得注意的是，CAMEL 框架正在经历快速的发展和演进。从其 [GitHub 仓库](https://github.com/camel-ai/camel) 可以看到，CAMEL 已经远不止是一个简单的双智能体协作框架，目前已经具备：

- ​**多模态能力**：支持文本、图像、音频等多种模态的智能体协作
- ​**工具集成**：内置了丰富的工具库，包括搜索、计算、代码执行等
- ​**模型适配**：支持 OpenAI、Anthropic、Google、开源模型等多种 LLM 后端
- ​**生态联动**：与 LangChain、CrewAI、AutoGen 等主流框架实现了互操作性

主要局限性

1. 对提示工程的高度依赖

CAMEL 的成功很大程度上取决于初始提示的质量。这带来了几个挑战：

- ​**提示设计门槛**：需要深入理解目标领域和 LLM 的行为特性
- ​**调试复杂性**：当协作效果不佳时，很难定位是角色定义、任务描述还是交互规则的问题
- ​**一致性挑战**：不同的 LLM 对相同提示的理解可能存在差异

2. 协作规模的限制

虽然 CAMEL 在双智能体协作上表现出色，但在处理大规模多智能体场景时面临挑战：

- ​**对话管理**：缺乏像 AutoGen 那样的复杂对话路由机制
- ​**状态同步**：没有 AgentScope 那样的分布式状态管理能力
- ​**冲突解决**：当多个智能体意见分歧时，缺乏有效的仲裁机制

3. 任务适用性的边界

CAMEL 特别适合需要深度协作和创造性思维的任务，但在某些场景下可能不是最优选择：

- ​**严格流程控制**：对于需要精确步骤控制的任务，LangGraph 的图结构更合适
- ​**大规模并发**：AgentScope 的消息驱动架构在高并发场景下更有优势
- ​**复杂决策树**：AutoGen 的群聊模式在多方决策场景下更加灵活

我个人感觉, CAMEL适合做一些小的项目.

# LangGraph

久仰大名, 但是看上去好像很简洁.

维护一个全局状态, 然后就是"节点"负责干活, 能够获取和更新全局状态, "边"负责连接节点. 边还有"条件边", 你可以自己写判断函数, 决定其在不同条件下跳转到不同的节点.

（1）优势

- 如我们的智能搜索助手案例所示，LangGraph 将一个完整的实时问答流程，显式地定义为一个由状态、节点和边构成的“流程图”。这种设计的最大优势是​**高度的可控性与可预测性**​。开发者可以精确地规划智能体的每一步行为，这对于构建需要高可靠性和可审计性的生产级应用至关重要。其最强大的特性在于对​**循环（Cycles）的原生支持**。通过条件边，我们可以轻松构建“反思-修正”循环，例如在我们的案例中，如果搜索失败，可以设计一个回退到备用方案的路径。这是构建能够自我优化和具备容错能力的智能体的关键。
- 此外，由于每个节点都是一个独立的 Python 函数，这带来了​**高度的模块化**。同时，在流程中插入一个等待人类审核的节点也变得非常直接，为实现可靠的“人机协作”（Human-in-the-loop）提供了坚实的基础。

（2）局限性

- 与基于对话的框架相比，LangGraph 需要开发者编写更多的​**前期代码（Boilerplate）** ​。定义状态、节点、边等一系列操作，使得对于简单任务而言，开发过程显得更为繁琐。开发者需要更多地思考“如何控制流程（how）”，而不仅仅是“做什么（what）”。由于工作流是预先定义的，LangGraph 的行为虽然可控，但也缺少了对话式智能体那种动态的、​ **“涌现”式的交互**。它的强项在于执行一个确定的、可靠的流程，而非模拟开放式的、不可预测的社会性协作。
- 调试过程同样存在挑战。虽然流程比对话历史更清晰，但问题可能出在多个环节：某个节点内部的逻辑错误、在节点间传递的状态数据发生异变，或是边跳转的条件判断失误。这要求开发者对整个图的运行机制有全局性的理解。

有点像UE蓝图呃呃.

# 总结

一路看下来感觉最猛的是agentScope, 最简单的是CAMEL, 最像写代码的是LangGraph, 最像团队的是AutoGen.

‍

第七章还实现了自己的Agent框架HelloAgents. 在此也一并介绍.

HelloAgents的架构比较简洁, 除了核心的Agent类，一切皆为Tools。在许多其他框架中需要独立学习的Memory（记忆）、RAG（检索增强生成）、RL（强化学习）、MCP（协议）等模块，在HelloAgents中都被统一抽象为一种“工具”。这种设计的初衷是消除不必要的抽象层，让学习者可以回归到最直观的“智能体调用工具”这一核心逻辑上，从而真正实现快速上手和深入理解的统一。

```python
hello-agents/
├── hello_agents/
│   │
│   ├── core/                     # 核心框架层
│   │   ├── agent.py              # Agent基类
│   │   ├── llm.py                # HelloAgentsLLM统一接口
│   │   ├── message.py            # 消息系统
│   │   ├── config.py             # 配置管理
│   │   └── exceptions.py         # 异常体系
│   │
│   ├── agents/                   # Agent实现层
│   │   ├── simple_agent.py       # SimpleAgent实现
│   │   ├── react_agent.py        # ReActAgent实现
│   │   ├── reflection_agent.py   # ReflectionAgent实现
│   │   └── plan_solve_agent.py   # PlanAndSolveAgent实现
│   │
│   ├── tools/                    # 工具系统层
│   │   ├── base.py               # 工具基类
│   │   ├── registry.py           # 工具注册机制
│   │   ├── chain.py              # 工具链管理系统
│   │   ├── async_executor.py     # 异步工具执行器
│   │   └── builtin/              # 内置工具集
│   │       ├── calculator.py     # 计算工具
│   │       └── search.py         # 搜索工具
└──
```

core负责接入llm, agents作为中端来处理上下文, 显示日志, 调用llm, 解析llm的回复, 调用对应工具等等. 然后tools就很好理解, 不再赘述.

值得一提的是HelloAgent后续更新, 新增了FunctionCallAgent. 这玩意好像说是OpenAI原生函数调用机制.

我看了下, 好像就是把tools中注册的工具转为function call要求的结构(Schema), 然后就可以在调用llm的时候额外将这个结构作为参数传进去. 然后从返回的message的tool_calls字段拿到llm想调用的函数, 然后做调用.

其他的都是一些细节问题了, 不在此赘述.

‍
